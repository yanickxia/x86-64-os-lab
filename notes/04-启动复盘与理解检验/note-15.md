# 第 15 课学习记录

日期：2026.08.10

本章是纯总结，不新增代码、命令或红绿灯。用自己的话把下面四部分串起来即可；不要求背完整寄存器值。

## 1. 从 reset 到 kernel_start

沿真实执行顺序总结：CPU reset、BIOS、boot sector、实模式初始化与读盘、A20/GDT、保护模式、页表、long mode、跳入 `0x10000` 的 ELF 载荷。

我的总结：

## 2. 三次控制权移交

分别说明：

- BIOS 如何把控制权交给 boot sector。
- boot code 为什么用 far jump 完成 16→32 位和 32→64 位模式切换。
- bootloader 如何在 `CS` 不变的情况下把控制权交给独立内核载荷。

我的总结：

## 3. 交给 C 前已经成立的状态

不抄完整数值，说明这些状态分别解决了什么问题：long mode、平坦分段、四级分页、低 2 MiB 恒等映射、`0x90000` 栈、`0x10000` 加载/链接地址契约。

我已经确认：`CR0.PG=1`、`CR0.PE=1`。

我的总结：

## 4. 还缺哪些真正的内核基础设施

至少串起 IDT、`.bss`、栈保护、物理内存图、更完整的页表和正式设备驱动。特别说明为什么当前没有有效 IDT 却暂时还能运行，以及地址 0 已映射时空指针访问为什么不一定产生 `#PF`。

我的总结：

## 延伸思考（可选）

- 为什么开启分页的下一次取指要求当前低地址仍有映射？
- 为什么当前 BIOS `INT 13h` 读盘必须在保护模式之前，而 ATA/块设备驱动不受这个限制？
- 如果内核增长到 40 KiB，启动加载路径需要怎样演进？
- 将来怎样给内核栈增加保护页？

## 仍然不清楚的问题

-
