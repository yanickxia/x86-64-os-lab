# 第 16 课学习记录

日期：2026.08.10

本章是跨架构讨论，不新增代码或验收命令。重点不是背寄存器名，而是建立角色映射。

## 1. 哪些是 x86-64 特有的历史路径

从 A20、实模式、GDT、模式切换等内容中，写出我认为换到 RISC-V/ARM64 后会消失或明显改变的部分：

我的总结：

## 2. 三种架构共同需要哪些 OS 机制

从特权隔离、地址空间、trap、系统调用、上下文切换、多核同步和设备访问中，写出我认为最重要的共同点：

我的总结：

## 3. 把 xv6 RISC-V 翻译成 x86-64 角色

至少选择三组概念，例如 `satp ↔ CR3`、`stvec ↔ IDT`、`sepc ↔ saved RIP`、`sfence.vma ↔ TLB invalidation`。说明它们解决的共同问题，也指出为什么不能逐字等同。

我的总结：

## 4. 如果把当前内核移植到另一架构

哪些代码必须重写？哪些将来的 C 子系统应该尽量保持不变？

我的总结：

## 仍然不清楚的问题

- 
